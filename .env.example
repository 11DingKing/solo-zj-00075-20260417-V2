# 数据库连接字符串（可选，有默认值）
# DATABASE_URL=mongodb://mongodb:27017/stackoverflow-clone

# JWT 密钥（强烈建议修改为自己的密钥）
JWT_SECRET=your_secure_jwt_secret_key_here

# 前端网站名称/域名
SITE_NAME=localhost:3000

# API 地址（前端调用后端的地址）
# 如果前端和后端在同一台机器上，使用 localhost
# 如果在生产环境，使用实际的域名或 IP
API_URL=http://localhost:8080/api
